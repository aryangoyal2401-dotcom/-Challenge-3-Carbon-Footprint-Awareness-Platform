version: '3.8'

services:
  ecotrack:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/usr/src/app/data
    environment:
      - PORT=5000
      - NODE_ENV=production
    restart: unless-stopped
